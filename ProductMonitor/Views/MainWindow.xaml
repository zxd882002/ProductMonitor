<Window x:Class="ProductMonitor.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}"
        Height="750"
        Width="1200"
        WindowStartupLocation="CenterScreen">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0" />
    </WindowChrome.WindowChrome>
    <Grid>
        <Grid.Background>
            <RadialGradientBrush>
                <GradientStop Color="#FF285173" Offset="0" />
                <GradientStop Color="#FF244967" Offset="0.5" />
                <GradientStop Color="#FF14273A" Offset="1" />
            </RadialGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderThickness="0, 0, 0, 1" BorderBrush="#5518AABD">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0" Source="../Resources/Images/Logo.png" Margin="10 7" />
                <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Title}" Foreground="White" FontSize="16" />
                    <TextBlock Text="{Binding SubTitle}" Foreground="White" FontSize="12" Margin="0, 2, 0, 0"/>
                </StackPanel>
            </Grid>
        </Border>

        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="ContentRegion" />
        <TextBlock Grid.Row="2" Text="Footer" />
    </Grid>
</Window>