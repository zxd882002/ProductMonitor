<Window x:Class="ProductMonitor.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}"
        Height="750"
        Width="1200"
        WindowStartupLocation="CenterScreen">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0" />
    </WindowChrome.WindowChrome>
    <Grid>
        <Grid.Background>
            <RadialGradientBrush>
                <GradientStop Color="#FF285173" Offset="0" />
                <GradientStop Color="#FF244967" Offset="0.5" />
                <GradientStop Color="#FF14273A" Offset="1" />
            </RadialGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>

        <!--Header-->
        <Border Grid.Row="0" BorderThickness="0, 0, 0, 1" BorderBrush="#5518AABD">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0" Source="../Resources/Images/Logo.png" Margin="10 7" />
                <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Title}" Foreground="White" FontSize="16" />
                    <TextBlock Text="{Binding SubTitle}" Foreground="White" FontSize="12" Margin="0, 2, 5, 0" />
                </StackPanel>
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="15" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Background="Transparent"
                                WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button Content="&#xe650;" Style="{StaticResource OperationButtonStyle}" Command="{Binding MinimizeCommand}" />
                        <Button Content="&#xe651;" Style="{StaticResource OperationButtonStyle}" Command="{Binding MaximizeCommand}" />
                        <Button Content="&#xe600;" Style="{StaticResource OperationButtonStyle}" Background="DarkRed"
                                Command="{Binding CloseCommand}" />
                    </StackPanel>
                    <Border Grid.Row="1" BorderThickness="0 1 0 1" BorderBrush="#5518aabd">
                        <Border.Background>
                            <VisualBrush TileMode="FlipXY" Viewport="0 0 7 7" ViewportUnits="Absolute">
                                <VisualBrush.Visual>
                                    <Line X1="0" Y1="10" X2="10" Y2="0" Stroke="Gray" StrokeThickness="1" />
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </Border.Background>
                    </Border>
                </Grid>
            </Grid>
        </Border>

        <!--Body-->
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="ContentRegion" />

        <!--Footer-->
        <Border Grid.Row="2" BorderThickness="0  1 0 0 " BorderBrush="#5518aabd">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0">
                    <Border.Background>
                        <VisualBrush TileMode="FlipXY" Viewport="0 0 7 7" ViewportUnits="Absolute">
                            <VisualBrush.Visual>
                                <Line X1="0" Y1="10" X2="10" Y2="0" Stroke="Gray" StrokeThickness="1" />
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Border.Background>
                </Border>
                
                <TextBlock Grid.Column="1" Text="{Binding Footer}" Margin="5 0" Foreground="White" FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>