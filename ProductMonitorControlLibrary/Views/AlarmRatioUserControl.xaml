<UserControl x:Class="ProductMonitorControlLibrary.Views.AlarmRatioUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ProductMonitorControlLibrary.Views"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:vm="clr-namespace:ProductMonitorControlLibrary.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:AlarmRatioUserControlViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <lvc:PieChart InnerRadius="35" Series="{Binding PieSeries}">
            <lvc:PieChart.Resources>
                <Style TargetType="lvc:PieSeries">
                    <Setter Property="DataLabels" Value="True" />
                    <Setter Property="LabelPosition" Value="OutsideSlice" />
                    <Setter Property="StrokeThickness" Value="0" />
                    <Setter Property="DataLabelsTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Point.SeriesView.Title}" Margin="0 0 5 0" Foreground="#44ffffff" />
                                    <TextBlock Text="{Binding Point.SeriesView.Values[0]}" Foreground="#44ffffff" />
                                </StackPanel>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </lvc:PieChart.Resources>
        </lvc:PieChart>
    </Grid>
</UserControl>