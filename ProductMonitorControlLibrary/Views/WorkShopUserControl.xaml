<UserControl x:Class="ProductMonitorControlLibrary.Views.WorkShopUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ProductMonitorControlLibrary.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Border BorderThickness="0 1 0 0" BorderBrush="#9918aabd" Background="#01a8aabd" Margin="5 5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Left"
                       Foreground="#18aabd" Margin="10 0"
                       Text="{Binding WorkShopName, RelativeSource={RelativeSource AncestorType={x:Type local:WorkShopUserControl}}}" />
            <TextBlock Grid.Column="1" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Right"
                       Margin="10 0">
                <Hyperlink Foreground="White" TextDecorations="None">[详情]</Hyperlink>
            </TextBlock>

            <StackPanel Grid.Column="0" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="机台总数" Foreground="White" FontSize="10" />
                <TextBlock
                    Text="{Binding Total, RelativeSource={RelativeSource AncestorType={x:Type local:WorkShopUserControl}}}"
                    HorizontalAlignment="Center" Foreground="#99ffffff" FontSize="30" Margin="0 5" />
            </StackPanel>

            <UniformGrid Grid.Column="1" Grid.Row="1" Rows="2" Columns="2">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock
                        Text="{Binding WorkingCount, RelativeSource={RelativeSource AncestorType={x:Type local:WorkShopUserControl}}}"
                        HorizontalAlignment="Center" Foreground="LightSeaGreen" FontSize="16" />
                    <TextBlock Text="作业" Foreground="#55ffffff" FontSize="10" Margin="0 5" />
                </StackPanel>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock
                        Text="{Binding WaitingCount, RelativeSource={RelativeSource AncestorType={x:Type local:WorkShopUserControl}}}"
                        HorizontalAlignment="Center" Foreground="DarkOrange" FontSize="16" />
                    <TextBlock Text="等待" Foreground="#55ffffff" FontSize="10" Margin="0 5" />
                </StackPanel>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock
                        Text="{Binding ErrorCount, RelativeSource={RelativeSource AncestorType={x:Type local:WorkShopUserControl}}}"
                        HorizontalAlignment="Center" Foreground="Red" FontSize="16" />
                    <TextBlock Text="故障" Foreground="#55ffffff" FontSize="10" Margin="0 5" />
                </StackPanel>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock
                        Text="{Binding StopCount, RelativeSource={RelativeSource AncestorType={x:Type local:WorkShopUserControl}}}"
                        HorizontalAlignment="Center" Foreground="Gray" FontSize="16" />
                    <TextBlock Text="停机" Foreground="#55ffffff" FontSize="10" Margin="0 5" />
                </StackPanel>
            </UniformGrid>
        </Grid>
    </Border>
</UserControl>